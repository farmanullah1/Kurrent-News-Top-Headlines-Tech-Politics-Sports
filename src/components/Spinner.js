import React from 'react';

const Spinner = () => (
  <div className="kurrent-spinner">
    <div className="spinner-ring"></div>
    <span className="spinner-label">Loading</span>
  </div>
);

export default Spinner;